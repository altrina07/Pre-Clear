import { CheckCircle, XCircle, AlertTriangle, Scale, Package, DollarSign, FileWarning, Truck } from 'lucide-react';

export function ConstraintsValidationWidget() {
  // Removed per UI request — do not render constraints/validation content
  return null;
}
